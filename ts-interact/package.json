{
  "name": "hub",
  "version": "1.0.0",
  "description": "TypeScript scripts to deploy and interact with contracts on Polkadot Hub testnet",
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "deploy": "ts-node src/deploy.ts",
    "interact": "ts-node src/interact.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.6",
    "solc": "^0.8.28",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
